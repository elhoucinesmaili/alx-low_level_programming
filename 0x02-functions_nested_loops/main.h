#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

/* _putchar - writes the character c to stdout */
int _putchar(char c);

/* 0. prints the alphabet in lowercase */
void print_alphabet(void);

/* 1. prints the alphabet 10 times */
void print_alphabet_x10(void);

/* 2. checks for lowercase character */
int _islower(int c);

/* 3. checks for alphabetic character */
int _isalpha(int c);

/* 4. prints the sign of a number */
int print_sign(int n);

/* 6. computes the absolute value of an integer */
int _abs(int);

/* 7. prints the last digit of a number */
int print_last_digit(int);

/* 8. prints every minute of the day of Jack Bauer */
void jack_bauer(void);

/* 9. prints the 9 times table, starting with 0 */
void times_table(void);

/* 10. adds two integers and returns the result */
int add(int a, int b);

/* 11. prints all natural numbers from n to 98 */
void print_to_98(int n);

/* 12. prints the n times table, starting with 0 */
void print_times_table(int n);
int main(void);
void print_fibonacci(int n);
void print_fibonacci(int n);
#endif /* MAIN_H */
